# Streamlit TODO App
Copilot アプリでの実装確認
PythonのWebフレームワークである **Streamlit** を使用して作成した、シンプルなTODOアプリケーションです。
HTMLやCSS、JavaScriptを一切使用せず、PythonコードのみでUI構築、状態（セッション）管理、およびデータの永続化を実現しています。

---

## 主な機能

- **タスクの追加**: 入力エリアに文字を入力して「追加」を押す、またはEnterキーを押すことでタスクを登録できます。
- **ステータス管理**: チェックボックスを切り替えることで、タスクの「未完了 / 完了」の状態を直感的に更新できます。完了したタスクには自動で打ち消し線が表示されます。
- **タスクの削除**: 不要になったタスクを削除ボタンで一覧から除外できます。
- **データの永続化**: 登録したデータは、同一フォルダ内に `todos.json` というJSONファイルとして自動保存されます。ブラウザのリロードや、Streamlitサーバーの再起動を行ってもデータは消えずに保持されます。
- **表示フィルター**: 「すべて」「未完了」「完了」のステータスごとにタスクを切り替えて表示できます。

---

## セットアップと起動手順

以下のステップに従って、ローカル環境でアプリケーションを起動できます。

### 1. プロジェクトフォルダの作成と移動

任意の場所にディレクトリを作成し、その中に移動します。

    mkdir streamlit-todo-app
    cd streamlit-todo-app

### 2. 仮想環境の作成と有効化

Pythonの仮想環境を作成し、有効化します。

**Windows (コマンドプロンプト):**

    python -m venv venv
    venv\Scripts\activate

**macOS / Linux:**

    python3 -m venv venv
    source venv/bin/activate

### 3. ライブラリのインストール

`streamlit` をインストールします。

    pip install streamlit

### 4. `app.py` の作成

プロジェクトのルートディレクトリに `app.py` という名前でファイルを新規作成し、後述する「ソースコード例」のコードをすべて貼り付けて保存してください。

### 5. アプリケーションの起動

以下のコマンドを実行してアプリケーションを起動します。

    streamlit run app.py

コマンド実行後、自動的にブラウザが立ち上がり、 `http://localhost:8501` にてアプリの操作が可能になります。

---

## ディレクトリ構成

開発が進むと、以下のようなフォルダ構成になります。

```text
copilot-app/
├── venv/                 # Python仮想環境（Git管理対象外）
├── app.py                # メインプログラム（上記ソースコード）
├── todos.json            # 保存されたデータ（タスク追加時に自動生成されます）
└── README.md             # 本ファイル
